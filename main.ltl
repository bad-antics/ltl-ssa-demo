// ltl-ssa-demo/main.ltl
//
// SSA construction + dominance-tree visualiser.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-ssa-demo v0.1.0")
    io::println("SSA construction + dominance-tree visualiser.")
    0
}
