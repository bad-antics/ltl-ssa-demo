// examples/demo.ltl — runnable demo for ltl_ssa_demo

import std::io
use ltl_ssa_demo as lib

fn main() {
    io::println("ltl_ssa_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}